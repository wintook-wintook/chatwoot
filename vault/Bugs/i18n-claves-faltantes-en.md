# i18n — Claves faltantes en inglés

## Problema

- `en/contactTracking.js` no existía → creado con todas las claves de `CONTACT_TRACKING.*`, y
  registrado en `en/index.js`.
- `en/contact.json` no tenía la sección `NOTES.MODAL.*` → agregada.

## Archivos de idioma custom registrados

- ES: `contactTracking.js`, `trackingTemplates.json`
- EN: `contactTracking.js` (creado), `trackingTemplates.json`

Ver rutas base en [[estructura-y-convenciones]].
